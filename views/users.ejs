<!DOCTYPE html>
<html>
  <head>
    <title>index</title>
  </head>
  <body>
    <h1>Users</h1>
    <div>
      <% users.forEach(function(user) { %>
      <h4><a href="/users/<%=user.id%>"><%= user.username %></a></h4>
      <% }) %>
    </div>
  </body>
</html>